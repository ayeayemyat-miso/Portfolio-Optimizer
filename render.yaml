# render.yaml
services:
  - type: web
    name: smart-portfolio-optimizer
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app.dashboard:server
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0